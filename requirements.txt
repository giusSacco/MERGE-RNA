# MERGE-RNA dependencies
# Note: ViennaRNA must be installed via conda (not pip):
#   conda install -c conda-forge -c bioconda viennarna
numpy>=1.20
pandas>=1.3
scipy>=1.7
matplotlib>=3.4
tqdm>=4.60
biopython>=1.79
